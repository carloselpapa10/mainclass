pipeline {
    agent { 
	docker {
            image 'maven:3-alpine'
        }
    }
    stages {
	
	stage('Docker Build'){
	    steps {
		sh 'mvn clean package docker:build'
            }	   
	}
	stage('Delivery'){
	    steps {
		sh './4. Testing/deliver.sh'
            }	   
	}
    }
}
