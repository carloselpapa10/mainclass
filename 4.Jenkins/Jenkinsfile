pipeline {
    agent none
    tools { 
        maven 'maven-3' 
    }
    stages {
	stage('Docker Compose') {
	      agent any
	      steps {
		sh 'docker-compose -f 4.Jenkins/docker-compose.yml up'
	      }
	}
    }
}
